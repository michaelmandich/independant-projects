How to use:
------------------
1. Open "Testing Zone" folder in command line interface. (Cd in command line until inside testing zone)
2. Run "Python 3.6 tester.py"
3. View results in the form of 6 csv files (sorted hexadecimally) and the time of completion of each set of keys in the command line. 